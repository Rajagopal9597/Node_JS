// let firstName = process.argv[2];
// let lastName = process.argv[3];
// process.argv() is a command used for returning all the command line arguments that 
// were passed before node.js is being launched

// console.log(process.argv);

// console.log(firstName + " " + lastName);

// process.env.NODE_ENV = "production";

// Node environment variable specifies the environment in which  an application 
// is running (devloment or production)

// if (process.env.NODE_ENV == "development") {
//     console.log("Connect to development database");

// }else {
//     console.log("Connect to Production database");
// }

console.log(process.env.APIKEYS);